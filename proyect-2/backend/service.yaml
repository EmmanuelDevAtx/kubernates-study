apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  namespace: development
spec:
  type: NodePort
  selector:
    app: backend
  ports:
    - protocol: TCP
      name: backend
      port: 3000
      targetPort: 3000