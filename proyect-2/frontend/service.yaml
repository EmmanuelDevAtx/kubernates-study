apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  namespace: development
spec:
  selector:
    app: frontend
  ports:
    - protocol: TCP
      name: frontend
      port: 3001
      targetPort: 3001